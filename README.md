# Docker for PHP

Common commands:

```
docker-compose up
docker-compose down

# Rebuild
docker-compose up --build

````

## Common endpoints

Your local is ready at: http://localhost:7000/

PHPMyAdmin: http://localhost:8000/

